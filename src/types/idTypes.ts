// TODO: Define exact id validation for all ids, using something like regex?
type InfographId = string;
type PageId = string;
type WidgetId = string;

export type { InfographId, PageId, WidgetId };
