import { screen } from '@testing-library/react';

describe('hook/useFontloader', () => {
  it('should load the font properly', async () => {
    // TODO: need to mock loadFonts
    // const fonts = ['Oxygen'];
    // const { result, waitForNextUpdate } = renderHook(() => useFontLoader(fonts));
    // await waitForNextUpdate();
    // expect(result.current).toBeTruthy();
  });
});
