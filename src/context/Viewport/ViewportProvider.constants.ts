// Default value for debounced resize event is set below
// A range of 200ms - 400ms should suffice to prevent the event callback from firing excessively.
// Can adjust or tinker a(as long as it's above the recommended minimum of 200ms) if there's any visual delay / jumpy-ness.
export const DEFAULT_RESIZE_EVENT_DEBOUNCE = 350;
