type GenericFunction = (...args: any[]) => any;

type TextStyle = { fontFamily: string; fontWeight: string; lineHeight: number | string };

type BoundingBoxRect = { width: number; height: number };

export type { GenericFunction, TextStyle, BoundingBoxRect };
