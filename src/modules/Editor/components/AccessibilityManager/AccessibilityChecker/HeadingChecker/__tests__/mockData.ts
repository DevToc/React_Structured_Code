import { generateDefaultData } from '../../../../../../../widgets/TextBasedWidgets/TextWidget/TextWidget.helpers';

const DEFAULT_TEXT = 'Default Text';

export const HEADING_TEXT_WIDGET_DATA = {
  1: generateDefaultData('heading 1', DEFAULT_TEXT).widgetData,
  2: generateDefaultData('heading 2', DEFAULT_TEXT).widgetData,
  3: generateDefaultData('heading 3', DEFAULT_TEXT).widgetData,
  4: generateDefaultData('heading 4', DEFAULT_TEXT).widgetData,
  5: generateDefaultData('heading 5', DEFAULT_TEXT).widgetData,
  6: generateDefaultData('heading 6', DEFAULT_TEXT).widgetData,
};
