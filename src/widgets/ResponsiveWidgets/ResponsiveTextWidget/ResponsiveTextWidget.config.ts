import { HANDLE } from 'constants/bounding-box';

const DEFAULT_FIXED_PADDING = 20;
const BOUNDING_BOX_CONFIG = {
  customHandle: HANDLE.CORNERS,
};

export { DEFAULT_FIXED_PADDING, BOUNDING_BOX_CONFIG };
